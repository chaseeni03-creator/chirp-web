VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-only — used by api/waitlist.js, never exposed to the client (do NOT
# prefix these with VITE_). Set them in the Vercel project's Environment
# Variables, not in this file.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
